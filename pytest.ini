[pytest]
markers =
    contract: Contract testing suite for PhotoChanger API
    integration: Integration testing suite for PhotoChanger queue and workers
    unit: Unit testing suite for domain and scaffolding helpers
    db_sensitive: Tests that perform schema-destructive PostgreSQL operations
addopts = -m "not db_sensitive"
