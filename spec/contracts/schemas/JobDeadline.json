{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "JobDeadline",
  "type": "object",
  "required": ["expires_at", "remaining_ms", "is_expired"],
  "properties": {
    "expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "Момент, когда задача обязана быть финализирована согласно DeadlineGuard."
    },
    "remaining_ms": {
      "type": "integer",
      "description": "Оставшееся время до дедлайна в миллисекундах (может быть отрицательным, если дедлайн уже истёк)."
    },
    "is_expired": {
      "type": "boolean",
      "description": "Признак того, что дедлайн уже наступил."
    }
  },
  "additionalProperties": false
}
